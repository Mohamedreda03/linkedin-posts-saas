# 📋 Workflow Implementation Plan - LinkedIn Automation SaaS

## 🎯 الهدف الرئيسي
بناء نظام متكامل لإنشاء ونشر وجدولة المنشورات على منصات التواصل الاجتماعي مع حفظ كامل البيانات وإدارة الأخطاء.

---

## 📊 نظرة عامة على المراحل

| المرحلة | الوصف | الأولوية | التقدير الزمني |
|---------|-------|----------|----------------|
| 1 | تحديث Schema قاعدة البيانات | 🔴 حرجة | 2-3 ساعات |
| 2 | نظام حفظ المنشورات | 🔴 حرجة | 3-4 ساعات |
| 3 | تحديث واجهة التحرير | 🔴 حرجة | 4-5 ساعات |
| 4 | نظام الجدولة | 🟡 مهمة | 5-6 ساعات |
| 5 | لوحة تحكم المنشورات | 🟡 مهمة | 4-5 ساعات |
| 6 | نظام رفع الوسائط | 🟢 تحسين | 3-4 ساعات |
| 7 | تجديد التوكنات | 🟢 تحسين | 3-4 ساعات |
| 8 | إدارة الأخطاء وإعادة المحاولة | 🟢 تحسين | 2-3 ساعات |

---

## 🔴 المرحلة 1: تحديث Schema قاعدة البيانات

**✅ الحالة**: القسم 1.2 مكتمل بالكامل | القسم 1.1 يحتاج إجراء يدوي  
**📄 راجع**: `STAGE_1_COMPLETED.md` للتفاصيل الكاملة

### 1.1 تحديث Appwrite Collections

#### الخطوة 1.1.1: تحديث مجموعة Posts
**الملف**: `lib/appwrite.ts` + Appwrite Console

**⚠️ هذه الخطوات يدوية - راجع APPWRITE_SETUP_GUIDE.md للتفاصيل**

**الحقول الجديدة المطلوبة:**
```
| اسم الحقل | النوع | الحجم | مطلوب | القيمة الافتراضية | الوصف |
|-----------|-------|-------|-------|-------------------|-------|
| status | string | 20 | ✅ | "draft" | draft|scheduled|publishing|published|failed |
| scheduledAt | string | 50 | ❌ | null | تاريخ الجدولة ISO |
| publishedPlatforms | string | 2000 | ❌ | "[]" | JSON array للمنصات المنشور عليها |
| errorLog | string | 2000 | ❌ | null | تفاصيل الأخطاء |
| mediaUrls | string | 2000 | ❌ | "[]" | JSON array لروابط الوسائط |
| retryCount | integer | - | ❌ | 0 | عدد محاولات إعادة النشر |
| lastRetryAt | string | 50 | ❌ | null | آخر محاولة إعادة |
```

- [x] **1.1.1.a** فتح Appwrite Console
- [x] **1.1.1.b** الانتقال إلى Database → Posts Collection
- [x] **1.1.1.c** إضافة حقل `status` (string, 20, required, default: "draft")
- [x] **1.1.1.d** إضافة حقل `scheduledAt` (string, 50, optional)
- [x] **1.1.1.e** إضافة حقل `publishedPlatforms` (string, 2000, optional, default: "[]")
- [x] **1.1.1.f** إضافة حقل `errorLog` (string, 2000, optional)
- [x] **1.1.1.g** إضافة حقل `mediaUrls` (string, 2000, optional, default: "[]")
- [x] **1.1.1.h** إضافة حقل `retryCount` (integer, optional, default: 0)
- [x] **1.1.1.i** إضافة حقل `lastRetryAt` (string, 50, optional)

#### الخطوة 1.1.2: إنشاء Indexes للأداء

**⚠️ هذه الخطوات يدوية - راجع APPWRITE_SETUP_GUIDE.md للتفاصيل**

- [x] **1.1.2.a** إنشاء Index على `status` (للفلترة السريعة)
- [x] **1.1.2.b** إنشاء Index على `scheduledAt` (للجدولة)
- [x] **1.1.2.c** إنشاء Index على `workspaceId` + `status` (composite)
- [x] **1.1.2.d** إنشاء Index على `userId` + `status` (composite)

### 1.2 تحديث Types في الكود

#### الخطوة 1.2.1: إنشاء ملف Types جديد
**الملف الجديد**: `lib/types/post.ts`

- [x] **1.2.1.a** إنشاء المجلد `lib/types/` إذا لم يكن موجوداً
- [x] **1.2.1.b** إنشاء الملف `lib/types/post.ts`
- [x] **1.2.1.c** تعريف `PostStatus` enum
- [x] **1.2.1.d** تعريف `Post` interface
- [x] **1.2.1.e** تعريف `PublishedPlatform` interface
- [x] **1.2.1.f** تعريف `CreatePostInput` interface
- [x] **1.2.1.g** تعريف `UpdatePostInput` interface

**المحتوى المطلوب:**
```typescript
// lib/types/post.ts

export type PostStatus = 'draft' | 'scheduled' | 'publishing' | 'published' | 'failed';

export interface PublishedPlatform {
  platform: 'linkedin' | 'twitter' | 'facebook' | 'instagram';
  postId: string;
  publishedAt: string;
  url?: string;
}

export interface Post {
  $id: string;
  $createdAt: string;
  $updatedAt: string;
  userId: string;
  workspaceId: string;
  content: string;
  topic: string;
  tone?: string;
  status: PostStatus;
  scheduledAt?: string;
  publishedPlatforms: PublishedPlatform[];
  errorLog?: string;
  mediaUrls: string[];
  retryCount: number;
  lastRetryAt?: string;
  isPublished: boolean; // للتوافق مع الكود القديم
  publishedTo?: string;
  publishedAt?: string;
}

export interface CreatePostInput {
  userId: string;
  workspaceId: string;
  content: string;
  topic: string;
  tone?: string;
  status?: PostStatus;
  scheduledAt?: string;
  mediaUrls?: string[];
}

export interface UpdatePostInput {
  content?: string;
  topic?: string;
  tone?: string;
  status?: PostStatus;
  scheduledAt?: string;
  publishedPlatforms?: PublishedPlatform[];
  errorLog?: string;
  mediaUrls?: string[];
  retryCount?: number;
  lastRetryAt?: string;
}
```

#### الخطوة 1.2.2: إنشاء ملف index للتصدير
**الملف الجديد**: `lib/types/index.ts`

- [x] **1.2.2.a** إنشاء الملف `lib/types/index.ts`
- [x] **1.2.2.b** تصدير جميع الـ types من `post.ts`

---

## 🔴 المرحلة 2: نظام حفظ المنشورات

### 2.1 تحديث دوال Appwrite

#### الخطوة 2.1.1: تحديث `lib/appwrite.ts`

- [x] **2.1.1.a** استيراد Types الجديدة
- [x] **2.1.1.b** تحديث دالة `createPost` لتشمل الحقول الجديدة
- [x] **2.1.1.c** إنشاء دالة `updatePost` جديدة
- [x] **2.1.1.d** إنشاء دالة `getPostById`
- [x] **2.1.1.e** إنشاء دالة `getPostsByWorkspace`
- [x] **2.1.1.f** إنشاء دالة `getPostsByStatus` (مدمجة في getPostsByWorkspace)
- [x] **2.1.1.g** إنشاء دالة `getScheduledPosts`
- [x] **2.1.1.h** إنشاء دالة `deletePost`
- [x] **2.1.1.i** إنشاء دالة `updatePostStatus`

**الدوال المطلوبة:**
```typescript
// إضافة إلى lib/appwrite.ts

// إنشاء منشور جديد
export async function createPost(input: CreatePostInput): Promise<Post>

// تحديث منشور
export async function updatePost(postId: string, input: UpdatePostInput): Promise<Post>

// جلب منشور بالـ ID
export async function getPostById(postId: string): Promise<Post | null>

// جلب منشورات حسب الـ workspace
export async function getPostsByWorkspace(
  workspaceId: string, 
  status?: PostStatus,
  limit?: number,
  offset?: number
): Promise<{ posts: Post[]; total: number }>

// جلب المنشورات المجدولة الجاهزة للنشر
export async function getScheduledPostsReadyToPublish(): Promise<Post[]>

// تحديث حالة المنشور
export async function updatePostStatus(
  postId: string, 
  status: PostStatus, 
  additionalData?: Partial<UpdatePostInput>
): Promise<Post>

// حذف منشور
export async function deletePost(postId: string): Promise<void>
```

### 2.2 إنشاء API Routes للمنشورات

#### الخطوة 2.2.1: إنشاء CRUD API للمنشورات
**الملف الجديد**: `app/api/posts/route.ts`

- [ ] **2.2.1.a** إنشاء مجلد `app/api/posts/`
- [ ] **2.2.1.b** إنشاء الملف `route.ts`
- [ ] **2.2.1.c** تنفيذ `GET` - جلب المنشورات (مع فلترة وpagination)
- [ ] **2.2.1.d** تنفيذ `POST` - إنشاء منشور جديد

**Query Parameters للـ GET:**
```
- workspaceId (required): معرف الـ workspace
- status (optional): فلترة حسب الحالة
- limit (optional): عدد النتائج (default: 20)
- offset (optional): للـ pagination
```

#### الخطوة 2.2.2: إنشاء API للمنشور الواحد
**الملف الجديد**: `app/api/posts/[id]/route.ts`

- [ ] **2.2.2.a** إنشاء مجلد `app/api/posts/[id]/`
- [ ] **2.2.2.b** إنشاء الملف `route.ts`
- [ ] **2.2.2.c** تنفيذ `GET` - جلب منشور واحد
- [ ] **2.2.2.d** تنفيذ `PATCH` - تحديث منشور
- [ ] **2.2.2.e** تنفيذ `DELETE` - حذف منشور

#### الخطوة 2.2.3: إنشاء API للنشر
**الملف الجديد**: `app/api/posts/[id]/publish/route.ts`

- [ ] **2.2.3.a** إنشاء مجلد `app/api/posts/[id]/publish/`
- [ ] **2.2.3.b** إنشاء الملف `route.ts`
- [ ] **2.2.3.c** تنفيذ `POST` - نشر المنشور للمنصات المحددة

**منطق النشر:**
```
1. جلب المنشور من DB
2. تغيير الحالة إلى "publishing"
3. لكل منصة محددة:
   a. استدعاء API المنصة المناسبة
   b. في حالة النجاح: إضافة للـ publishedPlatforms
   c. في حالة الفشل: تسجيل الخطأ
4. تحديث الحالة النهائية (published/failed/partial)
5. إرجاع النتيجة
```

---

## 🔴 المرحلة 3: تحديث واجهة التحرير

### 3.1 تحديث صفحة Compose

#### الخطوة 3.1.1: تحديث `app/ws/[workspaceId]/compose/page.tsx`

- [ ] **3.1.1.a** استيراد Types الجديدة
- [ ] **3.1.1.b** إضافة state للـ `postId` (للمنشور المحفوظ)
- [ ] **3.1.1.c** إضافة state للـ `isSaving`
- [ ] **3.1.1.d** إضافة state للـ `scheduledAt`
- [ ] **3.1.1.e** إنشاء دالة `saveDraft` - حفظ كمسودة
- [ ] **3.1.1.f** تحديث دالة `handlePublish` - حفظ ثم نشر
- [ ] **3.1.1.g** إنشاء دالة `handleSchedule` - حفظ وجدولة
- [ ] **3.1.1.h** إضافة Auto-save كل 30 ثانية (اختياري)

**Flow الجديد للنشر:**
```
المستخدم يكتب المحتوى
        ↓
يختار المنصات
        ↓
يضغط "Save Draft" أو "Publish" أو "Schedule"
        ↓
┌─────────────────────────────────────────────────────┐
│ Save Draft:                                         │
│   1. POST /api/posts (status: draft)                │
│   2. حفظ postId في state                            │
│   3. عرض رسالة نجاح                                 │
└─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────┐
│ Publish Now:                                        │
│   1. إذا لم يكن هناك postId:                        │
│      - POST /api/posts (status: draft)              │
│   2. POST /api/posts/{id}/publish                   │
│   3. تحديث UI بالنتيجة                              │
└─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────┐
│ Schedule:                                           │
│   1. إذا لم يكن هناك postId:                        │
│      - POST /api/posts (status: scheduled)          │
│   2. أو PATCH /api/posts/{id} (status: scheduled)   │
│   3. عرض رسالة تأكيد الجدولة                        │
└─────────────────────────────────────────────────────┘
```

### 3.2 تحديث مكونات Post Generator

#### الخطوة 3.2.1: تحديث Header Component
**الملف**: `components/post-generator/header.tsx`

- [ ] **3.2.1.a** إضافة زر "Save Draft"
- [ ] **3.2.1.b** إضافة Dropdown للـ "Publish" يشمل "Publish Now" و "Schedule"
- [ ] **3.2.1.c** إظهار حالة الحفظ (Saved/Saving/Unsaved)
- [ ] **3.2.1.d** إظهار آخر وقت حفظ

#### الخطوة 3.2.2: إنشاء مكون Schedule Dialog
**الملف الجديد**: `components/post-generator/schedule-dialog.tsx`

- [ ] **3.2.2.a** إنشاء الملف
- [ ] **3.2.2.b** تصميم UI لاختيار التاريخ والوقت
- [ ] **3.2.2.c** التحقق من صحة التاريخ (مستقبلي)
- [ ] **3.2.2.d** دعم Timezone
- [ ] **3.2.2.e** زر تأكيد الجدولة

#### الخطوة 3.2.3: تحديث Types
**الملف**: `components/post-generator/types.ts`

- [ ] **3.2.3.a** إضافة `PostGeneratorProps` الجديدة
- [ ] **3.2.3.b** إضافة `SaveStatus` type
- [ ] **3.2.3.c** تحديث الـ exports

---

## 🟡 المرحلة 4: نظام الجدولة

### 4.1 إنشاء Scheduler Service

#### الخطوة 4.1.1: إنشاء ملف Scheduler
**الملف الجديد**: `lib/scheduler.ts`

- [ ] **4.1.1.a** إنشاء الملف
- [ ] **4.1.1.b** إنشاء دالة `processScheduledPosts`
- [ ] **4.1.1.c** إنشاء دالة `publishToAllPlatforms`
- [ ] **4.1.1.d** إنشاء دالة `handlePublishError`
- [ ] **4.1.1.e** إضافة logging للتتبع

**منطق الـ Scheduler:**
```typescript
async function processScheduledPosts() {
  // 1. جلب المنشورات المجدولة والجاهزة
  const posts = await getScheduledPostsReadyToPublish();
  
  for (const post of posts) {
    try {
      // 2. تغيير الحالة إلى publishing
      await updatePostStatus(post.$id, 'publishing');
      
      // 3. نشر على جميع المنصات
      const results = await publishToAllPlatforms(post);
      
      // 4. تحديث النتائج
      await updatePostWithResults(post.$id, results);
      
    } catch (error) {
      // 5. معالجة الأخطاء
      await handlePublishError(post.$id, error);
    }
  }
}
```

### 4.2 إنشاء Cron Job API

#### الخطوة 4.2.1: إنشاء Cron Endpoint
**الملف الجديد**: `app/api/cron/process-scheduled/route.ts`

- [ ] **4.2.1.a** إنشاء مجلد `app/api/cron/process-scheduled/`
- [ ] **4.2.1.b** إنشاء الملف `route.ts`
- [ ] **4.2.1.c** تنفيذ `GET` handler
- [ ] **4.2.1.d** إضافة Authorization header check (للأمان)
- [ ] **4.2.1.e** استدعاء `processScheduledPosts`
- [ ] **4.2.1.f** إرجاع تقرير التنفيذ

#### الخطوة 4.2.2: إعداد Vercel Cron
**الملف**: `vercel.json`

- [ ] **4.2.2.a** إنشاء أو تحديث `vercel.json`
- [ ] **4.2.2.b** إضافة cron configuration

**المحتوى:**
```json
{
  "crons": [
    {
      "path": "/api/cron/process-scheduled",
      "schedule": "* * * * *"
    }
  ]
}
```

#### الخطوة 4.2.3: إضافة Environment Variables
- [ ] **4.2.3.a** إضافة `CRON_SECRET` للتحقق من الطلبات
- [ ] **4.2.3.b** تحديث `.env.example`
- [ ] **4.2.3.c** إضافة المتغير في Vercel Dashboard

### 4.3 بديل: Appwrite Functions (اختياري)

#### الخطوة 4.3.1: إنشاء Appwrite Function
**الملف الجديد**: `functions/process-scheduled/index.js`

- [ ] **4.3.1.a** إنشاء مجلد `functions/process-scheduled/`
- [ ] **4.3.1.b** إنشاء `index.js`
- [ ] **4.3.1.c** إعداد Appwrite Function في Console
- [ ] **4.3.1.d** جدولة التشغيل كل دقيقة

---

## 🟡 المرحلة 5: لوحة تحكم المنشورات

### 5.1 تحديث صفحة Workspace الرئيسية

#### الخطوة 5.1.1: تحديث `app/ws/[workspaceId]/page.tsx`

- [ ] **5.1.1.a** إضافة استدعاء API لجلب المنشورات
- [ ] **5.1.1.b** إضافة Tabs للفلترة (All, Drafts, Scheduled, Published, Failed)
- [ ] **5.1.1.c** عرض قائمة المنشورات
- [ ] **5.1.1.d** إضافة Pagination
- [ ] **5.1.1.e** إضافة زر "New Post"

### 5.2 إنشاء مكونات العرض

#### الخطوة 5.2.1: إنشاء Post Card Component
**الملف الجديد**: `components/workspace/post-card.tsx`

- [ ] **5.2.1.a** إنشاء الملف
- [ ] **5.2.1.b** عرض محتوى المنشور (مقتطف)
- [ ] **5.2.1.c** عرض الحالة (badge ملون)
- [ ] **5.2.1.d** عرض المنصات المستهدفة/المنشور عليها
- [ ] **5.2.1.e** عرض تاريخ الإنشاء/الجدولة/النشر
- [ ] **5.2.1.f** أزرار الإجراءات (Edit, Delete, Retry, View)

#### الخطوة 5.2.2: إنشاء Posts List Component
**الملف الجديد**: `components/workspace/posts-list.tsx`

- [ ] **5.2.2.a** إنشاء الملف
- [ ] **5.2.2.b** عرض قائمة Post Cards
- [ ] **5.2.2.c** معالجة حالة التحميل (Skeleton)
- [ ] **5.2.2.d** معالجة حالة القائمة الفارغة
- [ ] **5.2.2.e** دعم Grid و List view

#### الخطوة 5.2.3: إنشاء Posts Filter Component
**الملف الجديد**: `components/workspace/posts-filter.tsx`

- [ ] **5.2.3.a** إنشاء الملف
- [ ] **5.2.3.b** فلترة حسب الحالة
- [ ] **5.2.3.c** فلترة حسب المنصة
- [ ] **5.2.3.d** فلترة حسب التاريخ
- [ ] **5.2.3.e** بحث في المحتوى

### 5.3 صفحة تعديل المنشور

#### الخطوة 5.3.1: إنشاء صفحة Edit Post
**الملف الجديد**: `app/ws/[workspaceId]/posts/[postId]/edit/page.tsx`

- [ ] **5.3.1.a** إنشاء المجلدات المطلوبة
- [ ] **5.3.1.b** إنشاء الصفحة
- [ ] **5.3.1.c** جلب بيانات المنشور
- [ ] **5.3.1.d** إعادة استخدام Post Generator مع البيانات الموجودة
- [ ] **5.3.1.e** معالجة حالة المنشور غير موجود

---

## 🟢 المرحلة 6: نظام رفع الوسائط

### 6.1 إعداد Appwrite Storage

#### الخطوة 6.1.1: إنشاء Storage Bucket في Appwrite
- [ ] **6.1.1.a** فتح Appwrite Console
- [ ] **6.1.1.b** الانتقال إلى Storage
- [ ] **6.1.1.c** إنشاء Bucket جديد باسم "post-media"
- [ ] **6.1.1.d** إعداد الصلاحيات (Users can create, read their own)
- [ ] **6.1.1.e** تحديد الحد الأقصى لحجم الملف (10MB)
- [ ] **6.1.1.f** تحديد أنواع الملفات المسموحة (image/*, video/*)

#### الخطوة 6.1.2: إضافة دوال Storage في Appwrite
**الملف**: `lib/appwrite.ts`

- [ ] **6.1.2.a** إضافة `BUCKET_ID` constant
- [ ] **6.1.2.b** إنشاء دالة `uploadMedia`
- [ ] **6.1.2.c** إنشاء دالة `deleteMedia`
- [ ] **6.1.2.d** إنشاء دالة `getMediaUrl`
- [ ] **6.1.2.e** إنشاء دالة `getMediaPreview` (للصور المصغرة)

### 6.2 إنشاء API للوسائط

#### الخطوة 6.2.1: إنشاء Upload API
**الملف الجديد**: `app/api/media/upload/route.ts`

- [ ] **6.2.1.a** إنشاء مجلد `app/api/media/upload/`
- [ ] **6.2.1.b** إنشاء الملف `route.ts`
- [ ] **6.2.1.c** تنفيذ `POST` handler
- [ ] **6.2.1.d** التحقق من نوع الملف
- [ ] **6.2.1.e** التحقق من حجم الملف
- [ ] **6.2.1.f** رفع الملف وإرجاع URL

#### الخطوة 6.2.2: إنشاء Delete API
**الملف الجديد**: `app/api/media/[id]/route.ts`

- [ ] **6.2.2.a** إنشاء مجلد `app/api/media/[id]/`
- [ ] **6.2.2.b** إنشاء الملف `route.ts`
- [ ] **6.2.2.c** تنفيذ `DELETE` handler

### 6.3 إنشاء مكون رفع الوسائط

#### الخطوة 6.3.1: إنشاء Media Upload Component
**الملف الجديد**: `components/post-generator/media-upload.tsx`

- [ ] **6.3.1.a** إنشاء الملف
- [ ] **6.3.1.b** تصميم UI للسحب والإفلات
- [ ] **6.3.1.c** دعم اختيار ملفات متعددة
- [ ] **6.3.1.d** عرض معاينة الصور المرفوعة
- [ ] **6.3.1.e** شريط التقدم أثناء الرفع
- [ ] **6.3.1.f** زر حذف لكل صورة
- [ ] **6.3.1.g** التحقق من الحدود (عدد، حجم)

#### الخطوة 6.3.2: تحديث Editor Area
**الملف**: `components/post-generator/editor-area.tsx`

- [ ] **6.3.2.a** دمج Media Upload Component
- [ ] **6.3.2.b** تمرير mediaUrls للـ parent
- [ ] **6.3.2.c** تحديث معاينات المنصات لدعم الصور

---

## 🟢 المرحلة 7: تجديد التوكنات

### 7.1 تحديث OAuth Token Management

#### الخطوة 7.1.1: إنشاء Token Refresh Utilities
**الملف الجديد**: `lib/oauth/token-refresh.ts`

- [ ] **7.1.1.a** إنشاء مجلد `lib/oauth/`
- [ ] **7.1.1.b** إنشاء الملف `token-refresh.ts`
- [ ] **7.1.1.c** إنشاء دالة `refreshLinkedInToken`
- [ ] **7.1.1.d** إنشاء دالة `refreshFacebookToken`
- [ ] **7.1.1.e** إنشاء دالة `refreshInstagramToken`
- [ ] **7.1.1.f** إنشاء دالة `refreshTwitterToken` (تحديث الموجودة)
- [ ] **7.1.1.g** إنشاء دالة عامة `refreshTokenIfNeeded`

**منطق التجديد:**
```typescript
async function refreshTokenIfNeeded(account: SocialAccount): Promise<SocialAccount> {
  // 1. التحقق من صلاحية التوكن
  if (isTokenValid(account.tokenExpiry)) {
    return account;
  }
  
  // 2. محاولة التجديد حسب المنصة
  switch (account.platform) {
    case 'linkedin':
      return await refreshLinkedInToken(account);
    case 'facebook':
    case 'instagram':
      return await refreshFacebookToken(account);
    case 'twitter':
      return await refreshTwitterToken(account);
  }
}
```

#### الخطوة 7.1.2: تحديث دوال النشر
**الملفات**: 
- `app/api/linkedin/post/route.ts`
- `app/api/facebook/post/route.ts`
- `app/api/instagram/post/route.ts`

- [ ] **7.1.2.a** إضافة استدعاء `refreshTokenIfNeeded` قبل النشر في كل ملف
- [ ] **7.1.2.b** معالجة حالة فشل التجديد
- [ ] **7.1.2.c** تحديث التوكن في قاعدة البيانات بعد التجديد

### 7.2 Cron Job لتجديد التوكنات

#### الخطوة 7.2.1: إنشاء Token Refresh Cron
**الملف الجديد**: `app/api/cron/refresh-tokens/route.ts`

- [ ] **7.2.1.a** إنشاء مجلد `app/api/cron/refresh-tokens/`
- [ ] **7.2.1.b** إنشاء الملف `route.ts`
- [ ] **7.2.1.c** جلب التوكنات القريبة من الانتهاء (خلال 24 ساعة)
- [ ] **7.2.1.d** تجديد كل توكن
- [ ] **7.2.1.e** إرسال إشعار للمستخدم في حالة الفشل

#### الخطوة 7.2.2: تحديث Vercel Cron
**الملف**: `vercel.json`

- [ ] **7.2.2.a** إضافة cron job جديد للتجديد (كل 6 ساعات)

---

## 🟢 المرحلة 8: إدارة الأخطاء وإعادة المحاولة

### 8.1 نظام إعادة المحاولة

#### الخطوة 8.1.1: إنشاء Retry Logic
**الملف الجديد**: `lib/retry.ts`

- [ ] **8.1.1.a** إنشاء الملف
- [ ] **8.1.1.b** إنشاء دالة `retryWithBackoff`
- [ ] **8.1.1.c** تحديد الحد الأقصى للمحاولات (3)
- [ ] **8.1.1.d** تنفيذ Exponential Backoff
- [ ] **8.1.1.e** تصنيف الأخطاء (قابلة للإعادة / غير قابلة)

**أنواع الأخطاء:**
```typescript
// قابلة للإعادة (Retryable)
- Rate limit errors
- Timeout errors
- 5xx Server errors
- Network errors

// غير قابلة للإعادة (Non-retryable)
- Invalid token (401)
- Permission denied (403)
- Not found (404)
- Invalid content (400)
```

#### الخطوة 8.1.2: تحديث Publish APIs
**الملفات**: جميع ملفات `app/api/*/post/route.ts`

- [ ] **8.1.2.a** تغليف استدعاءات API بـ `retryWithBackoff`
- [ ] **8.1.2.b** إرجاع معلومات الخطأ التفصيلية

### 8.2 واجهة إعادة المحاولة

#### الخطوة 8.2.1: إنشاء Retry API
**الملف الجديد**: `app/api/posts/[id]/retry/route.ts`

- [ ] **8.2.1.a** إنشاء مجلد `app/api/posts/[id]/retry/`
- [ ] **8.2.1.b** إنشاء الملف `route.ts`
- [ ] **8.2.1.c** التحقق من حالة المنشور (failed فقط)
- [ ] **8.2.1.d** التحقق من عدد المحاولات
- [ ] **8.2.1.e** إعادة محاولة النشر

#### الخطوة 8.2.2: إضافة زر Retry في UI
**الملف**: `components/workspace/post-card.tsx`

- [ ] **8.2.2.a** إضافة زر "Retry" للمنشورات الفاشلة
- [ ] **8.2.2.b** عرض عدد المحاولات المتبقية
- [ ] **8.2.2.c** عرض رسالة الخطأ الأخير

### 8.3 نظام الإشعارات

#### الخطوة 8.3.1: إنشاء Notification System (اختياري)
**الملف الجديد**: `lib/notifications.ts`

- [ ] **8.3.1.a** إنشاء الملف
- [ ] **8.3.1.b** دالة `notifyPublishSuccess`
- [ ] **8.3.1.c** دالة `notifyPublishFailure`
- [ ] **8.3.1.d** دالة `notifyTokenExpiring`

---

## 📝 ملاحظات التنفيذ

### الترتيب الموصى به للتنفيذ:
1. ✅ المرحلة 1 (Schema) - **أساسي**
2. ✅ المرحلة 2 (حفظ المنشورات) - **أساسي**
3. ✅ المرحلة 3 (واجهة التحرير) - **أساسي**
4. ⏳ المرحلة 5 (لوحة التحكم) - **مهم للاستخدام**
5. ⏳ المرحلة 4 (الجدولة) - **ميزة رئيسية**
6. ⏳ المرحلة 8 (الأخطاء) - **لتجربة أفضل**
7. ⏳ المرحلة 6 (الوسائط) - **لدعم Instagram**
8. ⏳ المرحلة 7 (التوكنات) - **للاستقرار طويل المدى**

### Environment Variables المطلوبة:
```env
# Existing
NEXT_PUBLIC_APPWRITE_ENDPOINT=
NEXT_PUBLIC_APPWRITE_PROJECT_ID=
APPWRITE_API_KEY=

# New
APPWRITE_DATABASE_ID=
APPWRITE_POSTS_COLLECTION_ID=
APPWRITE_SOCIAL_ACCOUNTS_COLLECTION_ID=
APPWRITE_WORKSPACES_COLLECTION_ID=
APPWRITE_WORKSPACE_ACCOUNTS_COLLECTION_ID=
APPWRITE_MEDIA_BUCKET_ID=
CRON_SECRET=
```

### اعتبارات الأمان:
- [ ] التحقق من ملكية المنشور قبل أي عملية
- [ ] التحقق من صلاحية المستخدم للـ workspace
- [ ] عدم تسريب التوكنات في الـ responses
- [ ] Rate limiting للـ APIs
- [ ] Validation لجميع المدخلات

### اعتبارات الأداء:
- [ ] استخدام Indexes في Appwrite
- [ ] Caching للبيانات المتكررة
- [ ] Pagination لقوائم المنشورات
- [ ] تحميل lazy للصور

---

## 🔄 تتبع التقدم

### المرحلة 1: تحديث Schema
- [x] 1.1.1 تحديث Posts Collection
- [x] 1.1.2 إنشاء Indexes
- [x] 1.2.1 إنشاء Types
- [x] 1.2.2 تصدير Types

### المرحلة 2: نظام حفظ المنشورات
- [x] 2.1.1 تحديث دوال Appwrite
- [ ] 2.2.1 إنشاء CRUD API
- [ ] 2.2.2 إنشاء Single Post API
- [ ] 2.2.3 إنشاء Publish API

### المرحلة 3: تحديث واجهة التحرير
- [ ] 3.1.1 تحديث Compose Page
- [ ] 3.2.1 تحديث Header
- [ ] 3.2.2 إنشاء Schedule Dialog
- [ ] 3.2.3 تحديث Types

### المرحلة 4: نظام الجدولة
- [ ] 4.1.1 إنشاء Scheduler
- [ ] 4.2.1 إنشاء Cron Endpoint
- [ ] 4.2.2 إعداد Vercel Cron
- [ ] 4.2.3 Environment Variables

### المرحلة 5: لوحة التحكم
- [ ] 5.1.1 تحديث Workspace Page
- [ ] 5.2.1 إنشاء Post Card
- [ ] 5.2.2 إنشاء Posts List
- [ ] 5.2.3 إنشاء Posts Filter
- [ ] 5.3.1 إنشاء Edit Page

### المرحلة 6: رفع الوسائط
- [ ] 6.1.1 إنشاء Storage Bucket
- [ ] 6.1.2 دوال Storage
- [ ] 6.2.1 Upload API
- [ ] 6.2.2 Delete API
- [ ] 6.3.1 Media Upload Component
- [ ] 6.3.2 تحديث Editor Area

### المرحلة 7: تجديد التوكنات
- [ ] 7.1.1 Token Refresh Utilities
- [ ] 7.1.2 تحديث دوال النشر
- [ ] 7.2.1 Refresh Cron
- [ ] 7.2.2 تحديث Vercel Cron

### المرحلة 8: إدارة الأخطاء
- [ ] 8.1.1 Retry Logic
- [ ] 8.1.2 تحديث Publish APIs
- [ ] 8.2.1 Retry API
- [ ] 8.2.2 Retry UI
- [ ] 8.3.1 Notifications

---

**آخر تحديث**: December 9, 2025
**الإصدار**: 1.0
